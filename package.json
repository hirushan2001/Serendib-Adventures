{
  "name": "serendib-adventures-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Serendib Adventures Sri Lanka Monorepo - Frontend, Backend, Admin",
  "workspaces": [
    "frontend",
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently -k -n \"FRONTEND,BACKEND,ADMIN\" -c \"cyan.bold,green.bold,magenta.bold\" \"npm run dev:frontend\" \"npm run dev:backend\" \"npm run dev:admin\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:admin": "npm run dev --workspace=admin",
    "build": "npm run build --workspaces --if-present",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:admin": "npm run build --workspace=admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
